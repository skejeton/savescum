import ("../../src/d_otenv.um"; "../../src/d_test.um")

fn main() {
  d_test.isUnitTest(true)

  input := "  test = true\n"+
           "dbg.world = true\n"+
           "  fps = 60\n"+
           "\tstyle = cute\r\n"+
           "help=me to get out of russia"

  output := d_otenv.parseKeyValuePairs(input)
  for i in output {
    printf("%-16s = %-16s\n", output[i][0], output[i][1])
  } 
}